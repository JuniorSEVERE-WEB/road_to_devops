version: '3'

services:
  web:
    image: nginx
    ports:
      - "8080:80"
  
  db:
    image: postgres
    environment:
      POSTGRESQL_PASSWORD: example
    volumes:
      - dbdata:/var/lib/postgresql/data

  volumes:
    dbdata:
